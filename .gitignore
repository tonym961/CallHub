*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/app/build/
/captures/
.externalNativeBuild/
.cxx/
*.apk
# Published sideload APK lives in the repo (auto-update reads it via raw URL)
!releases/*.apk
*.keystore
!debug.keystore
# Signing secrets — never commit
keystore.properties
/.config/
